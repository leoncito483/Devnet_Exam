erick@erick:~/labs/devnet-src/ansible/ansible-csr-backup$ more ansible.cfg 
ini
[defaults]
# Inventaire par défaut
inventory = inventory/hosts.ini

# Utilisateur par défaut pour la connexion
remote_user = admin

# Désactiver la vérification des clés SSH (pour un lab uniquement)
host_key_checking = False

# Timeout de connexion SSH
timeout = 30

# Éviter de gather facts inutiles
gathering = explicit

# Dossier des collections
collections_paths = ~/.ansible/collections

# Logs
log_path = ./ansible.log

[ssh_connection]
# Accélérer les connexions SSH
pipelining = True

[defaults]
inventory = inventory/hosts.ini
host_key_checking = False
timeout = 30
gathering = explicit
retry_files_enabled = False
stdout_callback = yaml
deprecation_warnings = False

[ssh_connection]
pipelining = True
erick@erick:~/labs
